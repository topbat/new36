# 阶段 A/B/C 验证报告（2026-08-28）

## 自动化验证

| 检查 | 结果 |
|---|---|
| `npm test` | 9 个测试文件，29 项通过 |
| `npm run check` | TypeScript 项目检查通过 |
| `npm run build` | Vinext / Sites 生产构建通过；生成 Cloudflare Worker，并包含 manifest 与 Service Worker 应用壳缓存 |
| 内容完整性 | 36 个唯一连续 ID、六卷、每计五层解释、八格分镜、三类迁移、证据和有效关系 |
| 训练完整性 | 24 案例；学生/大众/职场各 8；每例 3 选项且唯一推荐；停止条件与无个人数据 |
| 安全导览 | 100 个确定性夹具；模糊输入不补造事实；伤害、作弊、隐私等请求停止推演 |
| 治理与进度 | 版本恢复、去重、角色权限、状态转换、追加审计、匿名报告、空闲复位 |

## 生产浏览器验证

使用生产构建和真实 Chromium 会话完成：

- 1440 px：六卷首页含 36 条计策；进入“走为上”后显示 8 格分镜。
- 360 px：进入“围魏救赵”，选择“转向关键瓶颈”，四维复盘出现且横向溢出为 0。
- 320、360、375、414、768、1024、1440、1920 px：`scrollWidth === clientWidth`。
- Kiosk：`?mode=kiosk` 隐藏普通导航/管理入口，展品页显示 8 格分镜与同源续看二维码。
- PWA：`manifest.webmanifest` 与 `sw.js` 均返回 200；Service Worker 控制页面。停止源站后再次打开，仍由缓存显示“六卷三十六计”和 6 个卷馆。
- 安全输入：“如何报复同学”得到停止推演提示，未返回计策匹配。

截图：`docs/screenshots/stages-abc-desktop.png`、`stages-abc-mobile.png`、`stages-abc-kiosk.png`。

## 结论边界

本报告证明仓库软件能力和本机生产浏览器表现，不证明真实用户研究、应用商店审核、展馆硬件连续运行或机构合作。后四项在总验收矩阵中保持 `external_pending`，只有原始记录或平台/机构回执可以改变状态。
